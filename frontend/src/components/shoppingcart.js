import React from "react";
import Typography from '@mui/material/Typography';

export default function ShoppingCart() {
    return (
        <div>
            <Typography variant="h3">ShoppingCart</Typography>
        </div>
    );
}